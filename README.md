# NLPDisasterTweets
This project utilizes natural language processing via Recurrent Neural Networks (RNN), a deep learning methodology, to classify tweets as pertaining to a disaster occurrence or not.
